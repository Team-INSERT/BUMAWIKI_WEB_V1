import React from 'react';
import { Header } from 'src/allFiles';

const Teacher = () => {
    return (
        <div>
            <Header />
            선생님 목록
        </div>
    );
};

export default Teacher;